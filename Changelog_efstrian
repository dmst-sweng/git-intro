Clone the repository:
git clone https://github.com/dmst-sweng/git-intro.git

Add modified README.md to local repository (master):
git add README.md

Record changes:
git commit -m "Added and selected a task"

Push changes:
git push

Create and change to a new branch:
git checkout -b efstrian

Add modified ExcitingStory.java:
git add src/ExcitingStory.java

Commit changes to local repository (efstrian):
git commit -m "Added typing effect to sentence()"

Push changes to a new branch on the remote repository:
git push origin efstrian

Place changelog under version control:
git add Changelog_efstrian

Commit changes again:
git commit -m "Added changelog"

Push changes to efstrian branch on remote:
git push origin efstrian